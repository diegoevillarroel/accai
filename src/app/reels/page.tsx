import { AppLayout } from "@/components/layout/AppLayout";
import { Reels as ReelsPage } from "@/views/Reels";

export default function ReelsRoute() {
  return (
    <AppLayout>
      <ReelsPage />
    </AppLayout>
  );
}

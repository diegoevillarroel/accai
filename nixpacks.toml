[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm i -g pnpm && pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm next build"]

[start]
cmd = "pnpm next start -p ${PORT:-3000}"
